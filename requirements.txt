# Tapestry KB — dependencies grow per build phase (kept lean to avoid resolver conflicts).
# Step 1 (Connect & Explore) needs only Streamlit; the connectors use the Python stdlib.
streamlit>=1.36,<1.40      # <1.40 avoids the long-path template bundle on Windows Store Python

# attachment text extraction (Confluence PDFs/DOCX/PPTX). pdfplumber pinned to
# 0.11.6 — the newest that allows Pillow<11, so it coexists with streamlit 1.39.
pdfplumber==0.11.6
python-docx==1.2.0
python-pptx==1.0.2

# vector index (Step 3). Embeddings come from local Ollama via urllib (no client lib).
chromadb==1.5.9
numpy>=1.26

# Added in later phases:
#   anthropic   # Step 4 answer generation (if using Claude)
